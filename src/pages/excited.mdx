export const firstName = SEO.openGraph.profile.firstName
export const productTitle = SEO.title

export const Message = () => (
  <p>
    It's great that you want to get started. I'm excited to share {productTitle}{' '}
    with you.
  </p>
)

# Check Your Inbox!

<Message />

<div className="italic">
  Thanks,
  <br />
  {firstName}
</div>

import SEO from '../../next-seo.json'
import Layout from 'layouts'

export default ({children}) => (
  <Layout>
    <div className="prose dark:prose-dark prose-lg mx-auto py-24">
      {children}
    </div>
  </Layout>
)
